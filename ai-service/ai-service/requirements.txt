# ==========================================================
#  FinTack AI Agent Service - dependencies
#  Versions pinned to match the Day 1 / Day 2 course notebooks
# ==========================================================

# --- LangChain core (same versions as your course) ---
langchain==1.0.5
langchain-core==1.0.4
langchain-community==0.4.1
langchain-classic==1.0.0
langchain-text-splitters==1.0.0

# --- LLM provider (Groq) ---
langchain-groq==1.0.0

# --- LangGraph (transitive, but MUST be pinned) ---------------------------
# create_agent() is built on LangGraph. If pip is left to resolve these it
# picks versions that are incompatible with langchain-core==1.0.4 and the
# service dies on import with:
#     TypeError: Reviver.__init__() got an unexpected keyword 'allowed_objects'
# These three versions are a verified working set - do not loosen them.
langgraph==1.0.10
langgraph-prebuilt==1.0.8
langgraph-checkpoint==2.1.2

# --- Embeddings provider (Gemini - Groq has no embedding model) ---
langchain-google-genai==3.0.1

# --- Vector database ---
chromadb==1.3.4
langchain-chroma==1.0.0

# --- Web search tool ---
duckduckgo-search==8.1.1
ddgs==9.9.0

# --- Document loading (for RAG over PDFs / statements) ---
pypdf==6.2.0

# --- API server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Database + misc ---
supabase==2.11.0
requests==2.32.5
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
